/** @type {import('next-intl').NextIntlConfig} */
module.exports = {
  locales: ['es', 'en'],         // Idiomas soportados
  defaultLocale: 'es',           // Idioma por defecto
  messagesDirectory: './locales' // Carpeta donde están tus JSON
};